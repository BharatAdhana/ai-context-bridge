# AI Briefing — aibridge-context

> **Auto-generated by aibridge-context** | Last updated: 2026-06-11T11:49:15.662Z
> Paste this file into any AI assistant to immediately brief it on the full project state.


## 📋 Instructions For The AI Reading This

1. Read **Getting Started** to know how to install and run this project.
2. Read **Active Errors** first — fix those before anything else.
3. Read **Resolved Issues** before writing any fix — avoid re-introducing old bugs.
4. Read **Developer Notes In Code** for TODO/FIXME/HACK markers left by past sessions.
5. Read **Recent Code Changes** to understand what just changed and why.
6. Read **Code Catalogue** to know what each file/function/class does before calling anything.
7. Read **Module Dependencies** to understand how files connect before moving code around.
8. Read **API Routes** before adding or changing any endpoint.
9. Read **Dependencies** before suggesting `npm install` — the package may already exist.
10. Read **Working Context** to understand the current focus and locked-in decisions.
11. Read **What To Do Next** for prioritised next actions.

> This file is auto-generated by aibridge-context on every startup and file change.
> Do not edit it manually — changes will be overwritten.
> The full machine-readable state is in `.ai-context/state.json`.

## Project Overview

| Field | Value |
|---|---|
| **Project** | aibridge-context v2.1.0 |
| **Stage** | Production-ready |
| **Language** | Node.js |
| **Framework** | Express |
| **Runtime** | Node.js |
| **Package Manager** | npm |
| **TypeScript** | No |
| **Databases** | None detected |
| **Test Framework** | None detected |
| **Linter** | None |
| **Bundler** | None |
| **Cloud / Deploy** | None detected |
| **Last Updated** | 2026-06-11T11:49:15.359Z |
**Summary:** CLI tool with: ai-readable project context generation, cli automation workflow, automatic change tracking.
**Description:** Zero-config CLI that auto-generates an AI briefing file for any project — tracks code changes with real diffs, errors, fixes, file structure, API routes and dependencies. One command: npx aibridge-context start

## 🧭 Getting Started

**Install:** `npm install`
**Run:** `npm start`
**Entry point:** `scripts.start → node ./bin/cli.js start`

⚠️ **Environment:** 0/1 required variables configured
   No `.env` file found — create one with the variables listed in **Environment Variables** below.

## 🔴 Active Errors

_No active errors. All clear._



## 📌 Developer Notes In Code

_TODO/FIXME/HACK/XXX/BUG comments found in source — known gaps or risky areas flagged by past sessions._

- 🔧 **TODO** in `core/codeIntel.js:30` — / FIXME / HACK scanning
- 🔧 **TODO** in `New Updated Files/codeIntel.js:30` — / FIXME / HACK scanning

## 🚀 What To Do Next

**Recommended next steps:**
- Add automated tests


## 📝 Recent Code Changes

_No changes recorded yet._


## 📁 File Structure

```
├── bin
│   └── cli.js
├── core
│   ├── briefingGenerator.js
│   ├── codeDiff.js
│   ├── codeIntel.js
│   ├── fileSnapshot.js
│   ├── gitSync.js
│   ├── init.js
│   ├── stateManager.js
│   └── watcher.js
├── New Updated Files
│   ├── briefingGenerator.js
│   ├── codeDiff.js
│   ├── codeIntel.js
│   ├── fileSnapshot.js
│   ├── package.json
│   └── stateManager.js
├── server
│   ├── routes.js
│   └── server.js
├── templates
│   ├── brain.template.txt
│   ├── changelog.template.json
│   ├── context.template.md
│   └── state.template.json
├── utils
│   └── logger.js
├── index.js
├── LICENSE
├── package.json
├── prompt.md
└── README.md
```

## 🕸 Module Dependencies

_Internal file relationships — which project files import which other project files (external packages excluded)._

- `bin/cli.js` → `core/stateManager.js`, `core/init.js`, `core/gitSync.js`, `utils/logger.js`, `core/briefingGenerator.js`, `core/watcher.js`, `server/server.js`
- `core/init.js` → `core/stateManager.js`, `core/gitSync.js`
- `core/stateManager.js` → `core/codeDiff.js`, `core/fileSnapshot.js`, `core/codeIntel.js`, `core/briefingGenerator.js`
- `core/watcher.js` → `core/gitSync.js`, `core/fileSnapshot.js`, `core/stateManager.js`
- `index.js` → `core/init.js`, `core/watcher.js`, `core/briefingGenerator.js`, `core/stateManager.js`, `server/server.js`, `core/gitSync.js`
- `New Updated Files/stateManager.js` → `New Updated Files/codeDiff.js`, `New Updated Files/fileSnapshot.js`, `New Updated Files/codeIntel.js`, `New Updated Files/briefingGenerator.js`
- `server/routes.js` → `core/stateManager.js`
- `server/server.js` → `server/routes.js`

**Most-used internal modules:**
- `core/stateManager.js` — used by 5 files
- `core/gitSync.js` — used by 4 files
- `core/briefingGenerator.js` — used by 3 files
- `core/init.js` — used by 2 files
- `core/watcher.js` — used by 2 files
- `server/server.js` — used by 2 files
- `core/fileSnapshot.js` — used by 2 files
- `utils/logger.js` — used by 1 file

## 🔍 Code Catalogue

_Every source file with what it does — function/class purposes are pulled from docstrings and comments._

#### `bin/cli.js` (506 lines)
- **Functions:**
  - `sm()`
  - `initer()`
  - `gs()`
  - `parseArgs()` — ─────────────────────────────────────────────────────────────────
  - `readState()` — ─────────────────────────────────────────────────────────────────
  - `patchState()`
  - `prompt()`
  - `contextExists()`
  - `autoInit()` — ─────────────────────────────────────────────────────────────────
  - `autoGitSync()` — ─────────────────────────────────────────────────────────────────
  - `cmdStart()` — ─────────────────────────────────────────────────────────────────
  - `cmdInit()` — ─────────────────────────────────────────────────────────────────
  - `cmdUpdate()` — ─────────────────────────────────────────────────────────────────
  - `cmdLinkGithub()` — ─────────────────────────────────────────────────────────────────
  - `cmdBriefing()` — ─────────────────────────────────────────────────────────────────
  - `cmdError()` — ─────────────────────────────────────────────────────────────────
  - `cmdFix()`
  - `cmdNote()` — ─────────────────────────────────────────────────────────────────
  - `cmdFocus()`
  - `cmdDecide()`
  - `cmdQuestion()`
  - `cmdStatus()` — ─────────────────────────────────────────────────────────────────
  - `printHelp()` — ─────────────────────────────────────────────────────────────────
- **Imports:** path, fs, fs/promises, readline, ../core/stateManager, ../core/init, ../core/gitSync, ../utils/logger, ../core/briefingGenerator, ../core/watcher, ../server/server

#### `core/briefingGenerator.js` (480 lines)
- **Functions:**
  - `renderFileTree()` — ─────────────────────────────────────────────────────────────────
  - `fileTreeToString()`
  - `sectionHeader()` — ─────────────────────────────────────────────────────────────────
  - `buildSetupGuide()`
  - `buildCodeNotes()`
  - `buildDependencyGraphSection()`
  - `buildOverview()`
  - `buildActiveErrors()`
  - `buildResolvedIssues()`
  - `buildRecentChanges()`
  - `buildFileStructure()`
  - `buildCodeCatalogue()`
  - `buildApiRoutes()`
  - `buildDependencies()`
  - `buildEnvVariables()`
  - `buildArchitecture()`
  - `buildKeyFeatures()`
  - `buildWorkingContext()`
  - `buildNextSteps()`
  - `buildInstructions()`
  - `generateBriefing()` — ─────────────────────────────────────────────────────────────────
- **Exports:** generateBriefing
- **Imports:** path

#### `core/codeDiff.js` (381 lines)
- **Functions:**
  - `myersDiff()` — Myers diff – returns array of { op: 'eq'|'add'|'del', line }
  - `buildOps()`
  - `diffTexts()` — Produce a compact unified diff string from old/new text.
  - `buildSummary()`
  - `extractCodeSignals()` — Extract meaningful code-level signals from a diff patch.
  - `extractNoteSignals()` — Detects newly-added TODO / FIXME / HACK / XXX / BUG comments
  - `extractPySignals()`
  - `extractJsSignals()`
  - `extractJsonSignals()`
  - `uniqueSignals()`
- **Exports:** diffTexts, extractCodeSignals

#### `core/codeIntel.js` (251 lines)
- **Functions:**
  - `scanCodeNotes()` — Scans every tracked file for TODO/FIXME/HACK/XXX/BUG comments.
  - `buildDependencyGraph()` — Builds a map of project-file -> [project-files it imports],
  - `resolveRelativeImport()`
  - `resolvePythonImport()` — Resolves a dotted Python import (e.g. "signal_bot.signals.fusion" or
  - `tryPythonPathCandidates()`
  - `buildSetupGuide()` — Returns: {
  - `envFileExists()`
  - `readConfiguredEnvVarNames()`
- **Exports:** scanCodeNotes, buildDependencyGraph, buildSetupGuide
- **Imports:** fs, path

#### `core/fileSnapshot.js` (324 lines)
- **Functions:**
  - `captureSnapshot()` — ─────────────────────────────────────────────────────────────────
  - `getSnapshot()`
  - `readCurrentContent()`
  - `readFileSafe()`
  - `deleteSnapshot()`
  - `buildCodeFileCatalogue()` — For each tracked source file, extract:
  - `analyseSourceFile()`
  - `analyseJs()`
  - `captureJsSummary()` — Looks immediately above a function/class declaration for either a
  - `extractJsCommentSummary()`
  - `analysePython()`
  - `extractPythonDocstring()` — Looks at the line(s) immediately following a def/class for a
- **Imports:** fs, path

#### `core/gitSync.js` (325 lines)
- **Functions:** runGit, isGitRepository, hasRemote, getRemoteOriginUrl, getCurrentBranch, ensureMainBranch, hasStagedContextChanges, formatGitError, ensureGitInitialized, parseGitHubRepo, buildPublicAiUrls, logMissingRemoteInstructions, logPublicAiEndpoints, linkGithubRepository, syncContextToGit
- **Exports:** buildPublicAiUrls, ensureGitInitialized, getRemoteOriginUrl, isGitRepository, linkGithubRepository, syncContextToGit
- **Imports:** fs, path, child_process, util

#### `core/init.js` (159 lines)
- **Functions:** initProject, requestPublicSyncConsent, fileExists
- **Exports:** initProject
- **Imports:** path, fs/promises, ./stateManager, ./gitSync

#### `core/stateManager.js` (1007 lines)
- **Functions:**
  - `resolveRoot()` — ─────────────────────────────────────────────────────────────────
  - `getContextPaths()`
  - `isObj()` — ─────────────────────────────────────────────────────────────────
  - `uniq()`
  - `cap()`
  - `deepMerge()`
  - `normPath()`
  - `insideRoot()`
  - `shouldIgnoreProjectFile()` — ─────────────────────────────────────────────────────────────────
  - `scoreEvent()`
  - `scanFiles()` — ─────────────────────────────────────────────────────────────────
  - `buildFileTree()`
  - `readPkg()` — ─────────────────────────────────────────────────────────────────
  - `detectProjectMetadata()`
  - `extractRepoUrl()`
  - `detectTechStack()`
  - `anyFileExt()`
  - `detectPackageManager()`
  - `detectFramework()`
  - `detectDatabases()`
  - `detectTestFw()`
  - `detectBundler()`
  - `detectLinter()`
  - `detectCloud()`
  - `buildDependencyCatalogue()` — ─────────────────────────────────────────────────────────────────
  - `scanEnvVars()` — ─────────────────────────────────────────────────────────────────
  - `extractApiRoutes()` — ─────────────────────────────────────────────────────────────────
  - `createDefaultIssueTracker()` — ─────────────────────────────────────────────────────────────────
  - `applyErrorEvent()`
  - `applyResolveEvent()`
  - `buildCodeChangeEntry()` — Given a file path and its old/new content snapshots,
  - `bootstrapProjectAnalysis()` — ─────────────────────────────────────────────────────────────────
  - `collectAnalysisInputs()`
  - `detectSignals()`
  - `inferProjectType()`
  - `buildArchPatterns()`
  - `buildImplDetails()`
  - `buildKeyFeatures()`
  - `createDefaultChangelog()` — ─────────────────────────────────────────────────────────────────
  - `createDefaultState()`
- **Exports:** CONTEXT_DIR_NAME, DEFAULT_CONFIG, applyErrorEvent, applyResolveEvent, bootstrapProjectAnalysis, buildCodeFileCatalogue, buildDependencyCatalogue, buildFileTree, createDebouncedStateUpdater, createDefaultChangelog, createDefaultIssueTracker, createDefaultState, detectProjectMetadata, ensureContextDirectory, extractApiRoutes, getContextPaths, loadRuntimeConfig, readJsonFile, renderTemplate, scanEnvVars, scanFiles, scoreEvent, shouldIgnoreProjectFile, updateRuntimeConfig, updateProjectState, writeJsonAtomic, writeTextAtomic
- **Imports:** fs, fs/promises, path, ./codeDiff, ./fileSnapshot, ./codeIntel, ./briefingGenerator

#### `core/watcher.js` (122 lines)
- **Functions:** normPath, startWatcher
- **Exports:** startWatcher
- **Imports:** path, chokidar, ./gitSync, ./fileSnapshot, ./stateManager

#### `index.js` (44 lines)
- **Exports:** initProject, startWatcher, startServer, updateProjectState, loadRuntimeConfig, updateRuntimeConfig, createDefaultState, getContextPaths, bootstrapProjectAnalysis, generateBriefing, buildPublicAiUrls, ensureGitInitialized, linkGithubRepository, syncContextToGit
- **Imports:** ./core/init, ./core/watcher, ./core/briefingGenerator, ./core/stateManager, ./server/server, ./core/gitSync

#### `New Updated Files/briefingGenerator.js` (480 lines)
- **Functions:**
  - `renderFileTree()` — ─────────────────────────────────────────────────────────────────
  - `fileTreeToString()`
  - `sectionHeader()` — ─────────────────────────────────────────────────────────────────
  - `buildSetupGuide()`
  - `buildCodeNotes()`
  - `buildDependencyGraphSection()`
  - `buildOverview()`
  - `buildActiveErrors()`
  - `buildResolvedIssues()`
  - `buildRecentChanges()`
  - `buildFileStructure()`
  - `buildCodeCatalogue()`
  - `buildApiRoutes()`
  - `buildDependencies()`
  - `buildEnvVariables()`
  - `buildArchitecture()`
  - `buildKeyFeatures()`
  - `buildWorkingContext()`
  - `buildNextSteps()`
  - `buildInstructions()`
  - `generateBriefing()` — ─────────────────────────────────────────────────────────────────
- **Exports:** generateBriefing
- **Imports:** path

#### `New Updated Files/codeDiff.js` (381 lines)
- **Functions:**
  - `myersDiff()` — Myers diff – returns array of { op: 'eq'|'add'|'del', line }
  - `buildOps()`
  - `diffTexts()` — Produce a compact unified diff string from old/new text.
  - `buildSummary()`
  - `extractCodeSignals()` — Extract meaningful code-level signals from a diff patch.
  - `extractNoteSignals()` — Detects newly-added TODO / FIXME / HACK / XXX / BUG comments
  - `extractPySignals()`
  - `extractJsSignals()`
  - `extractJsonSignals()`
  - `uniqueSignals()`
- **Exports:** diffTexts, extractCodeSignals

#### `New Updated Files/codeIntel.js` (251 lines)
- **Functions:**
  - `scanCodeNotes()` — Scans every tracked file for TODO/FIXME/HACK/XXX/BUG comments.
  - `buildDependencyGraph()` — Builds a map of project-file -> [project-files it imports],
  - `resolveRelativeImport()`
  - `resolvePythonImport()` — Resolves a dotted Python import (e.g. "signal_bot.signals.fusion" or
  - `tryPythonPathCandidates()`
  - `buildSetupGuide()` — Returns: {
  - `envFileExists()`
  - `readConfiguredEnvVarNames()`
- **Exports:** scanCodeNotes, buildDependencyGraph, buildSetupGuide
- **Imports:** fs, path

#### `New Updated Files/fileSnapshot.js` (324 lines)
- **Functions:**
  - `captureSnapshot()` — ─────────────────────────────────────────────────────────────────
  - `getSnapshot()`
  - `readCurrentContent()`
  - `readFileSafe()`
  - `deleteSnapshot()`
  - `buildCodeFileCatalogue()` — For each tracked source file, extract:
  - `analyseSourceFile()`
  - `analyseJs()`
  - `captureJsSummary()` — Looks immediately above a function/class declaration for either a
  - `extractJsCommentSummary()`
  - `analysePython()`
  - `extractPythonDocstring()` — Looks at the line(s) immediately following a def/class for a
- **Imports:** fs, path

#### `New Updated Files/stateManager.js` (1007 lines)
- **Functions:**
  - `resolveRoot()` — ─────────────────────────────────────────────────────────────────
  - `getContextPaths()`
  - `isObj()` — ─────────────────────────────────────────────────────────────────
  - `uniq()`
  - `cap()`
  - `deepMerge()`
  - `normPath()`
  - `insideRoot()`
  - `shouldIgnoreProjectFile()` — ─────────────────────────────────────────────────────────────────
  - `scoreEvent()`
  - `scanFiles()` — ─────────────────────────────────────────────────────────────────
  - `buildFileTree()`
  - `readPkg()` — ─────────────────────────────────────────────────────────────────
  - `detectProjectMetadata()`
  - `extractRepoUrl()`
  - `detectTechStack()`
  - `anyFileExt()`
  - `detectPackageManager()`
  - `detectFramework()`
  - `detectDatabases()`
  - `detectTestFw()`
  - `detectBundler()`
  - `detectLinter()`
  - `detectCloud()`
  - `buildDependencyCatalogue()` — ─────────────────────────────────────────────────────────────────
  - `scanEnvVars()` — ─────────────────────────────────────────────────────────────────
  - `extractApiRoutes()` — ─────────────────────────────────────────────────────────────────
  - `createDefaultIssueTracker()` — ─────────────────────────────────────────────────────────────────
  - `applyErrorEvent()`
  - `applyResolveEvent()`
  - `buildCodeChangeEntry()` — Given a file path and its old/new content snapshots,
  - `bootstrapProjectAnalysis()` — ─────────────────────────────────────────────────────────────────
  - `collectAnalysisInputs()`
  - `detectSignals()`
  - `inferProjectType()`
  - `buildArchPatterns()`
  - `buildImplDetails()`
  - `buildKeyFeatures()`
  - `createDefaultChangelog()` — ─────────────────────────────────────────────────────────────────
  - `createDefaultState()`
- **Exports:** CONTEXT_DIR_NAME, DEFAULT_CONFIG, applyErrorEvent, applyResolveEvent, bootstrapProjectAnalysis, buildCodeFileCatalogue, buildDependencyCatalogue, buildFileTree, createDebouncedStateUpdater, createDefaultChangelog, createDefaultIssueTracker, createDefaultState, detectProjectMetadata, ensureContextDirectory, extractApiRoutes, getContextPaths, loadRuntimeConfig, readJsonFile, renderTemplate, scanEnvVars, scanFiles, scoreEvent, shouldIgnoreProjectFile, updateRuntimeConfig, updateProjectState, writeJsonAtomic, writeTextAtomic
- **Imports:** fs, fs/promises, path, ./codeDiff, ./fileSnapshot, ./codeIntel, ./briefingGenerator

#### `server/routes.js` (46 lines)
- **Functions:** createRoutes
- **Exports:** createRoutes
- **Imports:** path, express, ../core/stateManager

#### `server/server.js` (46 lines)
- **Functions:** startServer
- **Exports:** startServer
- **Imports:** express, ./routes

#### `utils/logger.js` (47 lines)
- **Functions:** createLogger
- **Exports:** createLogger


## 🛤 API Routes

| Method | Path | File |
|---|---|---|
| `GET` | `/state.json` | `server/routes.js` |
| `GET` | `/brain.txt` | `server/routes.js` |
| `GET` | `/context.md` | `server/routes.js` |
| `GET` | `/changelog.json` | `server/routes.js` |
| `GET` | `/briefing.md` | `server/routes.js` |
| `GET` | `/` | `server/routes.js` |

## 📦 Dependencies & Scripts

**Production:**
- `chokidar` ^4.0.3
- `express` ^5.1.0

**Scripts:**
| Name | Command |
|---|---|
| `start` | `node ./bin/cli.js start` |
| `init:context` | `node ./bin/cli.js init` |
| `update:context` | `node ./bin/cli.js update` |

## 🔑 Environment Variables

_All env vars referenced in source or .env files. Never invent undocumented ones. Status reflects whether a non-empty value is set in `.env` — values themselves are never read into this file._

| Variable | Status |
|---|---|
| `DEBUG` | ⚠️ not set |

## 🏗 Architecture & Implementation

**Patterns in use:**
- Command-line automation workflow
- Event-driven file watching pipeline
- REST API architecture
- Middleware-driven request pipeline
- Structured AI context delivery workflow
- Package-distributed CLI architecture

**Implementation details:**
- REST API with Express routing
- Express middleware pipeline
- Debounced file-system event watcher
- Structured AI context generation (state.json, brain.txt, context.md, changelog.json)
- Git-backed context sync workflow
- Node.js CLI entrypoint

## ⚡ Key Features

- AI-readable project context generation
- CLI automation workflow
- Automatic change tracking
- Optional GitHub sync for public AI access
- Local HTTP context server

---
_Generated by [aibridge-context](https://github.com/npm/aibridge-context) • 2026-06-11T11:49:15.662Z_