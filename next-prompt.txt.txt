The implementation intelligence layer is detecting frameworks correctly, but architecture_patterns and implementation_details are still empty in live state.json output.

This appears to be a state merge/persistence issue, not a detection issue.

Current evidence:

* framework detection works ("Express")
* API feature detection works
* watcher updates work
* but implementation-derived arrays are lost

Most likely cause:
watcher/state update pipeline overwrites bootstrap-derived fields with empty arrays during live updates.

Fix the state composition architecture.

Requirements:

1. Preserve bootstrap analysis results permanently unless re-analysis replaces them.

2. architecture_patterns and implementation_details must survive:

* watcher updates
* semantic update regeneration
* feature promotion
* dedupe passes

3. Ensure bootstrapProjectAnalysis() results are merged INTO state rather than replaced.

4. Verify the final pipeline works like:

bootstrap analysis
→ base project understanding

watcher intelligence
→ enrich existing state

NOT:
watcher intelligence
→ rebuild entire state from scratch

5. Ensure architecture_patterns and implementation_details are populated from:

* app.use
* router.get/post
* express.Router
* jwt.sign/verify
* mongoose.connect
* axios/fetch

6. Verify with live localhost output in a fresh Express test project.

Expected output should include:

"architecture_patterns": [
"REST API architecture",
"Middleware-driven request pipeline"
]

and meaningful implementation_details entries.

Do not redesign the intelligence engine.
Fix only state persistence/composition logic.
